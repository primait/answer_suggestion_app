<div class="well well-small">
  {{#if results}}
  <ul class="entries">
    {{#each results}}
    <li data-url="{{url}}">
      <a href="{{url}}">{{title}}</a>
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>{{t "no_results"}}
  {{/if}}
</div>
