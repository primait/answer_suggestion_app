<div class="well well-small">
  {{#if entries}}
  <ul class="entries">
    {{#each entries}}
    <li data-url="{{url}}">
      <a href="{{url}}" target="_blank">{{title}}</a>
    </li>
    {{/each}}
  </ul>
  {{/if}}
</div>
