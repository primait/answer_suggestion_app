<div class="well well-small">
  {{#if entries}}
  <ul class="entries">
    {{#each entries}}
    <li data-url="{{url}}">
      <a href="{{url}}">{{title}}</a>
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>{{t "no_entries"}}
  {{/if}}
</div>
