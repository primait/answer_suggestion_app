<div class="well well-small">
  {{#if entries}}
  <ul class="entries">
    {{#each entries}}
    <li>
      <a href="{{url}}" class="entry">{{title}}</a>
    </li>
    {{/each}}
  </ul>
  {{/if}}
</div>
